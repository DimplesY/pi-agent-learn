# 复制本文件为 .env 并填入你的 Key。
# Bun 会自动加载 .env，无需手动 dotenv。
#
# 方式 A（默认，使用内置 provider 集合）：
#   想用哪个服务商就填哪个。课程只用到的才需要填。
OPENAI_API_KEY=sk-xxx
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
OPENROUTER_API_KEY=

# 方式 B（如果用根目录 provider.tsx 的代理）：
#   把 OPENAI_API_KEY 填成「你的代理可用的 key」即可，
#   然后把 shared/config.ts 里的 getModels() 改为返回 getLocalModels()。
# OPENAI_API_KEY=sk-xxx
